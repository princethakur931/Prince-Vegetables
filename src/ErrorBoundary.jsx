import React from 'react'; export class ErrorBoundary extends React.Component { constructor(props) { super(props); this.state = { hasError: false, error: null }; } static getDerivedStateFromError(error) { return { hasError: true, error }; } componentDidCatch(error, errorInfo) { console.error('ErrorBoundary caught error:', error, errorInfo); } render() { if (this.state.hasError) { return <div style={{padding: '2rem', background: '#ffebee', color: '#cc0000', margin: '2rem', borderRadius: '8px', fontFamily: 'monospace'}}><h2>Something went wrong in React!</h2><pre style={{whiteSpace: 'pre-wrap'}}>{this.state.error?.toString()}</pre><p>Check the console for more details.</p></div>; } return this.props.children; } }